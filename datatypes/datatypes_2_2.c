#include <stdlib.h>
#include <stdio.h>

int main()
{
    int int_variable_1 = 67;
    int int_variable_2 = 100;
                        //%c is the placeholder for char variables, allows 
    //for changing int to char, not all intergers can be readable i.e.■▒ binary characters.
    printf("Printing the value of the first char variable: %c \n", int_variable_1);
    printf("Printing the value of the second char variable: %c \n", int_variable_2);
}