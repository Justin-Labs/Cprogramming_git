#include <stdlib.h>
#include <stdio.h>

int main()
{       //initailization, initailization; condition eval; update expression
    for (int index = 0, some_variable = 2002; index < 5; index++) {
        printf(" %d. Hello World (some_variable): %d\n", index, some_variable);
    }

    return EXIT_SUCCESS;
}