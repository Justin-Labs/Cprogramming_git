#include <stdlib.h>
#include <stdio.h>

int main()
{       //initailization; condition eval; update expression
    for (int index = 0 ; index < 5; index++) {
        printf(" %d. Hello World \n", index);
    }

    return EXIT_SUCCESS;
}