#include <stdlib.h>
#include <stdio.h>

int main()
{    //variable(value can be changed) = literal(value does not vary)
    int income = 100000;
    int expenses = 67000;

    printf("Initial income: %d \n", income);
    printf("Initial expenses: %d \n", expenses);

    printf( "-------------------\n\n");

    return EXIT_SUCCESS;
}